+-------------------------------------------------------------------------------
| Running ${PKGSTEM} on OpenBSD
+-------------------------------------------------------------------------------

The port creates a user _abagnale with home directory /var/abagnale. Maintenance
should be done by switching to that user.

Preparing the database
======================

The database schema will transfer all ownership to the abagnale database role.
That role must be created before creating the database.

	# su - _postgresql
	$ psql -U postgres
		CREATE ROLE abagnale;

Create the database by loading the SQL dump:

	# su - _postgresql
	$ psql -U postgres -f /usr/local/share/abagnale/database-postgresql.sql

The service then can be enabled and started by issueing:

	# rcctl enable ${PKGSTEM}
	# rcctl start ${PKGSTEM}

Maintenance
===========

The database should be vacuumed periodically by running abagnalectl vacuum.
To vacuum the database and to create backups of outdated data removed by
vacuum a script like the following may be added to /etc/daily.local.

	#!/bin/sh
	ABAGNALE_BACKUPDIR=/var/postgresql/abagnale/vacuum/$(date +%F)

	next_part "Performing abagnalectl vacuum"

	su _postgresql -c "mkdir -p $ABAGNALE_BACKUPDIR"
	su _abagnale -c "abagnalectl vacuum -a -b $ABAGNALE_BACKUPDIR"

